let person = {
    name: "mahendra sahu",
    age: 24,
    country: "India"
}

function logData() {
    let info = person.name + " is " + person.age + " years old and he lives in " + person.country;
    document.write(info);
}
logData()



// let info_el = document.getElementById("info");
// info_el.textContent = info;