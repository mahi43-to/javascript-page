// template string/literals

const recipient = "mahi"
const sender = "mahesh"

// const email = "Hey " + recipient + "! How is it going? Cheers Per"
const email = `Hey ${recipient} ! How is it going? Cheers ${sender}`
console.log(email)
document.write(email)